interface:
  display_name: "LocalLLM"
  short_description: "Repo-specific patterns and workflows for localLLM"
  default_prompt: "Use the localLLM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true